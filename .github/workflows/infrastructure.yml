name: Infrastructure

on:
  pull_request:
    types:
      - edited
      - opened
      - reopened
      - synchronize

permissions: {}

jobs:
  infrastructure:
    permissions:
      contents: read
      id-token: write # Permission ceiling for the main-only convergence job in the trusted reusable workflow.
    uses: collinbentley1/platform/.github/workflows/infrastructure.yml@e9babc092fa18fea4ca8028a59355d037c03a651
