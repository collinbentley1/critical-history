name: Infrastructure

on:
  pull_request:
    types:
      - edited
      - opened
      - reopened
      - synchronize

permissions: {}

jobs:
  infrastructure:
    permissions:
      contents: read
      id-token: write # Permission ceiling for the main-only convergence job in the trusted reusable workflow.
    uses: collinbentley1/platform/.github/workflows/infrastructure.yml@2051ad813caf41e205203d07190feb3012fa2667
