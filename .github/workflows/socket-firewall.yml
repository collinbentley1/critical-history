name: Socket Firewall

on:
  pull_request:
    types:
      - edited
      - opened
      - reopened
      - synchronize
  push:
    branches:
      - main

permissions: {}

jobs:
  firewall:
    name: Socket Firewall
    permissions:
      checks: read # Verify the exact successful checks from Socket App 156372.
      contents: read
    uses: collinbentley1/platform/.github/workflows/socket-firewall.yml@dbf956f3f9c2bbad252e2899e198ecf25c1cb64f
