name: Socket Firewall

on:
  pull_request:
    types:
      - edited
      - opened
      - reopened
      - synchronize
  push:
    branches:
      - main

permissions: {}

jobs:
  firewall:
    name: Socket Firewall
    permissions:
      checks: read # Verify the exact successful checks from Socket App 156372.
      contents: read
    uses: collinbentley1/platform/.github/workflows/socket-firewall.yml@f857a58e3ecac63b2b3882809efbfb7136bfa6c3
