name: Socket Firewall

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  firewall:
    name: Socket Firewall
    uses: collinbentley1/platform/.github/workflows/socket-firewall.yml@v0.1.2
    with:
      bun-version: canary
