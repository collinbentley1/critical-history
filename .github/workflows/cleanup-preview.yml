name: Cleanup preview

on:
  pull_request:
    types:
      - closed

permissions: {}

jobs:
  cleanup:
    permissions:
      id-token: write # Exchange only for the exact-SHA preview traffic operator.
    uses: collinbentley1/platform/.github/workflows/cleanup-preview.yml@d344f3835ca48f56c30a53641db8f3ea3a88ce53
