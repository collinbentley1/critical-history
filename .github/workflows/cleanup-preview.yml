name: Cleanup preview

on:
  pull_request:
    types:
      - closed

permissions: {}

jobs:
  cleanup:
    permissions:
      id-token: write # Exchange only for the exact-SHA preview traffic operator.
    uses: collinbentley1/platform/.github/workflows/cleanup-preview.yml@eb48f5f4e321b81bc033e1113bfe2f9e676f1484
