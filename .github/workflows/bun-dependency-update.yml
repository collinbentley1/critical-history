name: Update Bun dependencies

on:
  schedule:
    - cron: "23 13 * * 2"
  workflow_dispatch:

permissions: {}

jobs:
  propose:
    permissions:
      contents: write
      pull-requests: write
    uses: collinbentley1/platform/.github/workflows/bun-dependency-update.yml@dbf956f3f9c2bbad252e2899e198ecf25c1cb64f
